kind = "web"
previewPath = "/"
title = "NebulaX"
version = "1.0.0"
id = "artifacts/nebulax"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24875

[services.development]
run = "pnpm --filter @workspace/nebulax run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nebulax", "run", "build" ]
publicDir = "artifacts/nebulax/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24875"
BASE_PATH = "/"
